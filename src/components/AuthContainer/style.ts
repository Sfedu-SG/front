import { styled } from "@mui/material";

export const AuthContainer = styled("div")(
  () => `
    display: flex;
    flex-direction: column;
    align-items: center;
    max-width: 600px;
    margin: 0 auto;
    padding: 120px 40px;
`
);
